
{
	"notes":[
		"JSON files always start with either an array [] or an object {}",
		"JSON files cannot have comments, so put comments in this array",
		"Commas go between items and array/object but there should be a trailing comma",
		"All property names should be in quotation marks",
		"All string quotation marks must be double quotes",
		"Use a linter like jsonlint.com to check your JSON"
	]
}